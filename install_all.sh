# Set up the cross compilation envirioment variables for the buildroot toolchain
. ./cross_env.sh

# This script gets called to install compile-time generated files to the buildroot target directory.
# You would usually call "make install" here
